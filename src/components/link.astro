---
import { Icon } from 'astro-icon/components'
import NewTab from './new-tab.astro'

type Props = {
  href: string
  icon: string
  class?: string
}
const { href, icon, ...rest } = Astro.props
---

<NewTab {href} aria-label={icon.split(':').at(-1)}>
  <Icon name={icon} {...rest} />
</NewTab>
